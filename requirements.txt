# ── Runtime dependencies ────────────────────────────────────────────────
streamlit>=1.32
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26
plotly>=5.20
pytz
requests>=2.31
streamlit-autorefresh>=1.0.1

# ── Firebase Auth (REST API — no Admin SDK needed) ───────────────────
# No extra package required; uses `requests` (above) directly.

# ── Optional: NSE India fallback (gracefully skipped if absent) ─────
# nselib>=2.0

# ── Optional: News feed ────────────────────────────────────────
# newsapi-python>=0.2.7

# ── ML / scikit-learn (utils/ml_signals.py) ───────────────────────
scikit-learn>=1.3
joblib>=1.3

# ── PDF export (utils/export.py via df_to_pdf_bytes) ───────────────
reportlab>=4.0

# ── Twilio SMS alerts ────────────────────────────────────────
# twilio>=8.0
