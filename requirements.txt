# ── Runtime dependencies ────────────────────────────────────────────────────────
streamlit>=1.35
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26
plotly>=5.20
pytz
requests>=2.31
streamlit-autorefresh>=1.0.1

# ── Firebase Auth (REST API — no Admin SDK needed) ───────────────────────────
# No extra package required; uses `requests` (above) directly.

# ── ML / scikit-learn ────────────────────────────────────────────────────────
scikit-learn>=1.4
joblib>=1.3
scipy>=1.12

# ── PDF export ────────────────────────────────────────────────────────────────
reportlab>=4.0

# ── Optional: NSE India fallback ──────────────────────────────────────────────
# nselib>=2.0

# ── Optional: Twilio SMS alerts ───────────────────────────────────────────────
# twilio>=8.0

# ── Optional: News feed ───────────────────────────────────────────────────────
# newsapi-python>=0.2.7
