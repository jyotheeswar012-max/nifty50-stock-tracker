# DELETED — this module has moved to tabs/tab_alerts.py
# Streamlit was registering this as a duplicate page URL, causing a crash.
# The real implementation lives in tabs/ which is not auto-scanned by Streamlit.
raise ImportError("This file should not be imported. Use tabs/tab_alerts.py instead.")
