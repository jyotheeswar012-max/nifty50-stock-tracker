[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Ignore integration tests that require live network by default
markers =
    network: marks tests as requiring live internet (deselect with -m "not network")
