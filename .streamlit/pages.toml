# Streamlit multipage config — MINIMAL
# Only keep app.py as the main entry. All other navigation is via tabs inside app.py.

[[pages]]
path = "app.py"
name = "NSE Tracker"
icon = "🏦"

# Disable all other pages to stop the navigation crash
# (You can re-enable individual ones later once they are cleaned)
